= form_for @exam do |f|
  - if @exam.errors.any?
    #error_explanation
      %h2= t('exam.save.error', count: @exam.errors.count)
      %ul
        - @exam.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :name
    = f.text_field :name, :class => 'form-control'
  .form-group
    = f.label :intro_text
    = f.text_area :intro_text, :class => 'form-control', :rows => 5
  .form-group
    = f.submit t('save'), :class => 'btn btn-success'
