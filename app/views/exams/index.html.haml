%h1 Available exams
.row
  .col-md-12

    %table.table.table-striped
      %thead
        %tr
          %th Name
          %th Actions

      %tbody
        - @exams.each do |exam|
          %tr
            %td= link_to exam.name, exam
            %td
              = link_to edit_exam_path(exam) do
                %span.glyphicon.glyphicon-pencil
              = link_to exam, :method => :delete, :data => { :confirm => 'Are you sure?' } do
                %span.glyphicon.glyphicon-trash
